<%= form_with(model: mushroom, local: true) do |form| %>
  <% if mushroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mushroom.errors.count, "error") %> prohibited this mushroom from being saved:</h2>

      <ul>
        <% mushroom.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flavour %>
    <%= form.text_field :flavour %>
  </div>

  <div class="field">
    <%= form.label :pictures %>
    <%= form.file_field :pictures, multiple: true %>
  </div>

  #add a form field to buy and take current_user (devise) and assign it to this field

  #mushroom.buyer=current_user (in the controller)

  #make a view for your bought mushrooms 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
